# Sway configuration file for Wayland Laptop
# Sway is i3-compatible but for Wayland instead of X11
# Read `man 5 sway` for complete reference

### CORE SYSTEM SETUP ###
# (Wayland handles desktop portal differently - no explicit setup needed)

### VARIABLES ###

# Modifier key (Mod4 = Windows/Super key, Mod1 = Alt)
# NEEDED: Core navigation key for all shortcuts
# WITHOUT: You'd need to reconfigure every keybinding
set $mod Mod4

# Vim-style directional keys
# OPTIONAL: Convenience variables for consistent navigation
# WITHOUT: You'd use h,j,k,l directly in each binding
set $left h
set $down j
set $up k
set $right l

# Default terminal emulator
# NEEDED: Quick terminal access
# WITHOUT: Must type full terminal command each time
set $term alacritty

# Application launcher command (xargs swaymsg exec for Wayland compatibility)
# NEEDED: Quick app launching without terminal
# WITHOUT: Must use terminal to launch all programs
set $menu rofi -show run | xargs swaymsg exec --

# File search launcher using locate
# OPTIONAL: Quick file searching and opening
# WITHOUT: Must use terminal or file manager
set $filemenu locate home media | rofi -threads 0 -width 90 -dmenu -i -p "locate" | xargs -r -0 xdg-open

# Workspace names (not using variables like i3 config, but could)
# Direct numbers used here - simpler but less flexible

### APPEARANCE ###

# Window title font (Sway uses system defaults better than i3)
# System font is usually fine, explicit config often not needed

# Gap between windows
# OPTIONAL: Visual breathing room between windows
# WITHOUT: Windows touch each other (traditional tiling look)
gaps inner 10

### AUTOSTART APPLICATIONS ###

# (Sway doesn't use dex - systemd user services or sway config for autostart)
# NetworkManager applet and other system tray apps started by systemd typically

### INPUT CONFIGURATION ###

# Keyboard layout configuration (Wayland-specific)
# LAPTOP-SPECIFIC: Custom keyboard layout for German umlauts
# WITHOUT: Default US layout only
input type:keyboard {
    xkb_layout us-german-umlaut  # Custom layout for bilingual use
    # Alternative: xkb_layout us,de with toggle key
}

# Touchpad configuration (Wayland handles input devices)
# LAPTOP-SPECIFIC: Touchpad gestures and behavior
# WITHOUT: Default touchpad behavior (often less comfortable)
input "1739:30383:DELL07E6:00_06CB:76AF_Touchpad" {
    dwt enabled           # Disable while typing - prevents accidental touches
    tap enabled          # Tap to click
    natural_scroll enabled    # Mac-style scrolling direction
    middle_emulation enabled  # Three-finger tap = middle click
    pointer_accel 1.0        # Mouse acceleration (1.0 = significant accel)
}

### OUTPUT CONFIGURATION ###

# Display configuration (Wayland-specific)
# LAPTOP-SPECIFIC: Configure laptop display
# WITHOUT: Default resolution and position
output eDP-1 pos 0 0 res 1920x1080
focus output eDP-1

# Wallpaper setting (Wayland-specific)
# OPTIONAL: Desktop background
# WITHOUT: Solid color background
output * bg ~/images/arch.png fill

# Multi-monitor setup (commented out - uncomment when external display connected)
# output DP-2 pos 0 0 res 2560x1440     # External monitor on left
# output eDP-1 pos 2560 0 res 1920x1080 # Laptop on right
# focus output DP-2                      # Focus external when connected

### IDLE CONFIGURATION ###

# Automatic lock and display power management
# IMPORTANT: Security and power saving
# WITHOUT: Screen stays on forever, never locks
exec swayidle -w \
    timeout 1800 'swaylock -f -c 000000' \
    timeout 1800 'swaymsg "output * dpms off"' \
    resume 'swaymsg "output * dpms on"' \
    before-sleep 'swaylock -f -c 000000'
# 1800 seconds = 30 minutes
# Locks screen and turns off display after 30min idle
# Locks before system sleep

### KEY BINDINGS - BASICS ###

# Open terminal
# ESSENTIAL: Primary way to interact with system
# WITHOUT: Would need to use launcher to start terminal
bindsym $mod+Return exec $term

# Close focused window
# ESSENTIAL: Need a way to close windows
# WITHOUT: Must use mouse to click X button or kill from terminal
bindsym $mod+Shift+q kill

# Open application launcher
# ESSENTIAL: Launch programs without terminal
# WITHOUT: Everything must be launched from terminal
bindsym $mod+d exec $menu

# Open file search launcher
# OPTIONAL: Quick file access
# WITHOUT: Must use terminal or file manager
bindsym $mod+Shift+d exec $filemenu

# Reload configuration file
# IMPORTANT: Apply config changes without restart
# WITHOUT: Must restart Sway to apply any config changes
bindsym $mod+Shift+c reload

# Exit Sway (with confirmation dialog)
# NEEDED: Graphical logout option
# WITHOUT: Must logout via terminal
bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -b 'Yes, exit sway' 'swaymsg exit'

### KEY BINDINGS - WINDOW FOCUS ###

# Move focus with vim keys
# ESSENTIAL: Navigate between windows
# WITHOUT: Can only use mouse to select windows
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# Move focused window with vim keys
# IMPORTANT: Rearrange windows in workspace
# WITHOUT: Cannot reposition windows without mouse
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

### KEY BINDINGS - WORKSPACES ###

# Switch to workspace 1-10
# ESSENTIAL: Multi-desktop workflow
# WITHOUT: Stuck on single workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# Move container to workspace 1-10
# ESSENTIAL: Organize windows across workspaces
# WITHOUT: Windows stuck on workspace where opened
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

### KEY BINDINGS - LAYOUT ###

# Split next window vertically
# USEFUL: Control window arrangement
# WITHOUT: Sway auto-splits based on window dimensions
bindsym $mod+v splitv

# Split next window horizontally  
# USEFUL: Control window arrangement
# WITHOUT: Sway auto-splits based on window dimensions
bindsym $mod+b splith

# Toggle fullscreen for focused container
# IMPORTANT: Maximize window temporarily
# WITHOUT: Cannot fullscreen windows
bindsym $mod+f fullscreen

# Change container layout (stacking = all windows stacked, titles visible)
# OPTIONAL: Different window organization styles
# WITHOUT: Only split layout available
bindsym $mod+s layout stacking

# Change container layout (tabbed = tabs on top, one visible)
# OPTIONAL: Different window organization styles
# WITHOUT: Only split layout available
bindsym $mod+w layout tabbed

# Toggle between split layouts
# OPTIONAL: Quick layout switching
# WITHOUT: Must choose specific layout each time
bindsym $mod+e layout toggle split

# Toggle between tiling and floating
# IMPORTANT: Some apps work better floating
# WITHOUT: All windows forced to tile
bindsym $mod+Shift+space floating toggle

# Toggle focus between tiling and floating windows
# USEFUL: Quick navigation between layers
# WITHOUT: Must click to focus floating windows
bindsym $mod+space focus mode_toggle

# Focus parent container
# ADVANCED: Navigate container hierarchy
# WITHOUT: Cannot select groups of windows
bindsym $mod+a focus parent

### KEY BINDINGS - SPECIAL KEYS ###

# Volume control with PulseAudio
# IMPORTANT: System volume control
# WITHOUT: Must use pavucontrol or terminal
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle

# Brightness control (laptop-specific)
# LAPTOP-ESSENTIAL: Screen brightness adjustment
# WITHOUT: Must use settings app or terminal
bindsym XF86MonBrightnessDown exec light -U 5
bindsym XF86MonBrightnessUp exec light -A 5

# Screenshot bindings (Wayland grim + swappy for annotation)
# USEFUL: Quick screenshots with editing
# WITHOUT: Must use screenshot apps from menu
bindsym F11 exec grim - | swappy -f -
bindsym Print exec grim - | swappy -f -
bindsym Shift+Print exec grim -g "$(slurp)" - | swappy -f -

### MOUSE BINDINGS ###

# Drag floating windows with $mod+mouse
# NEEDED: Move floating windows
# WITHOUT: Cannot reposition floating windows
floating_modifier $mod normal

### STATUS BAR ###

# Waybar - Modern Wayland status bar
# IMPORTANT: Shows workspaces, system info, tray
# WITHOUT: No visual workspace indicator or system info
bar {
    swaybar_command waybar  # Delegates to waybar (configured separately)
}

### INCLUDES ###

# Include system-wide Sway configuration drops
# OPTIONAL: Allows package managers to add configs
# WITHOUT: Must manually add all configuration here
include /etc/sway/config.d/*
